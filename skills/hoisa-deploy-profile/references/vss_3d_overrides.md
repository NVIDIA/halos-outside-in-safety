# VSS Warehouse — 3D (Sparse4D) Overrides 🚧

> **Under development.** The 3D (Sparse4D / multi-view 3D-tracking) profile is not
> yet supported by this skill. This file is a placeholder for the VSS-side overrides
> the 3D profile will need — analogous to `vss_2d_overrides.md` for 2D.

When the 3D profile lands, this will document the `warehouse-3d-app` DeepStream and
VST config differences required for an Isaac Sim source.

For now, use the **`sil`** (2D) profile — see `vss_2d_overrides.md`.
